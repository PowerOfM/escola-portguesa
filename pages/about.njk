---
title: Our School
layout: layout.njk
css: about
heroImage: /assets/images/about-hero.png
navPage: about
---

<section class="two-column">
  <div class="container two-column-grid">
    <div class="two-column-image">
      <img src="/assets/images/about-intro.png" alt="Escola Portuguesa" />
    </div>
    <div class="two-column-content">
      {% for paragraph in about.intro %}
      <p>{{ paragraph }}</p>
      {% endfor %}
    </div>
  </div>
</section>

<div class="page-divider"></div>

<section class="about-teachers">
  <div class="container">
    <h2>Our Teachers</h2>

    <div class="about-teachers-list">
      {% for teacher in teachers | sortByOrder %}
      <article class="teacher-card">
        <div class="teacher-card-image">
          {% if teacher.image %}
          <img src="{{ teacher.image }}" alt="{{ teacher.name }}" />
          {% else %}
          <div class="teacher-card-image-placeholder"></div>
          {% endif %}
        </div>
        <div class="teacher-card-text">
          <h3>{{ teacher.name }}</h3>
          {% for paragraph in teacher.bio %}
          <p>{{ paragraph }}</p>
          {% endfor %}
        </div>
      </article>
      {% endfor %}
    </div>
  </div>
</section>

<section class="about-volunteers section-tinted">
  <div class="container">
    <h2>{{ about.volunteers.heading }}</h2>
    {% for paragraph in about.volunteers.body %}
    <p>{{ paragraph }}</p>
    {% endfor %}
  </div>
</section>
